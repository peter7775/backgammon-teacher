# LLM Router Notes

- Use a small interface for providers.
- Keep routing separate from business logic.
- Route by use case, not by prompt text.
- Provide a local fallback for offline mode.
- Add tests for selection and fallback behavior.
